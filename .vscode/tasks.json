{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "pwsh cmake_build.ps1",
            "problemMatcher": "$msCompile"
        }
    ]
}